import React, { FC } from 'react';
import Container from '../Container';

const Loader: FC = () => {
  return (
    <Container>
      <div>Carregando...</div>
    </Container>
  );
};

export default Loader;
